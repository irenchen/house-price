# development
nodemon bin/www

# production
git init

# create an heroku personal app
heroku create

git add .

git commit -m '...'

# deploy app to heroku cloud server
git push heroku master

# create a new branch and switch to it
git checkout -b testing

